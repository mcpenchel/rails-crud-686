<h2>Check out all our Restaurants!</h2>

<ul>
  <% @restaurants.each do |restaurant| %>
    <li>
      <%= restaurant.name %> |
       <%= link_to "Delete", restaurant_path(restaurant.id), method: :delete %> |
       <%= link_to "Check details", restaurant_path(restaurant.id) %>
    </li>
  <% end %>
</ul>

<%= link_to "Create a new restaurant", new_restaurant_path %>
